# Pre-commit hooks for CourtListener Citation Validation MCP
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args:
          - '--baseline'
          - '.secrets.baseline'
          - '--exclude-files'
          - '\.md$'
          - '--exclude-files'
          - 'package-lock\.json'
          - '--exclude-files'
          - '\.lock$'
        exclude: ^(\.secrets\.baseline|\.prompt_injections\.baseline)$

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        exclude: ^(\.secrets\.baseline|\.prompt_injections\.baseline)$
      - id: end-of-file-fixer
        exclude: ^(\.secrets\.baseline|\.prompt_injections\.baseline)$
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-json
      - id: check-merge-conflict
      - id: detect-private-key

  - repo: local
    hooks:
      - id: prompt-injection-check
        name: Check for prompt injection patterns
        entry: uv run python .security/check_prompt_injections.py --baseline
        language: system
        files: \.(py|txt|md|yml|yaml|json|js|ts|html|xml|csv)$
        exclude: \.security/.*_detector\.py$
